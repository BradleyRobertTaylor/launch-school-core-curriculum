extends layout.pug 

block appContent
    h1 Bradley Taylor
    h2 You have: #{money}
    if !recentTransaction
      p No recent transactions.
    else
      p Recent transaction.
    button Log Out
